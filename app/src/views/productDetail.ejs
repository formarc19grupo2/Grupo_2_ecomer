<!DOCTYPE html>
<html lang="es">
    <%- include('partials/head.ejs') %>
<body>
    <!-- /* HEADER */ -->
    <%- include('partials/header.ejs') %>
    <main class="productDetail-container">
        <section class="productDetail">
            <div class="images-container">
                <!-- <div class="thumbnailImages">
                    <% product.images.forEach(image => { %>
                        <img src="/images/productos/<%- productoimage.image%>" alt="Imagen de producto1" onclick="myFunction(this);">
                    <% }) %>
                </div> -->
                <div class="mainImage">
                 <img src="/images/productos/<%= productoimage.image %>" alt="Imagen de producto2" id="expandedImg"> 
                </div>
            </div>
            <div action="/user/productCart" class="productData-container">
                <h3 class="productName"><%= product.name %> </h3>
                <div class="productPrice">$<%= product.price %> </div>
                <div class="productNumber">
                    
                    <div class="counter" id="quantity">
                        <button type="button" class="btn-subtract" onclick="subtract();">
                            -
                        </button>
                        <div id="count">1</div>
                                
                        <button type="button" id="btn-add" onclick="add();">
                            +
                        </button>
                    </div>
                    
                </div>
                
                <% if (session.user !== undefined) { %>
                    <div class="btn btn-ad-primary" id="btnAddToCart" >Agregar al carrito</div>
                    <% } else { %>
                    <div class="btn btn-ad-primary" id="btnAddToCartNotLogin">Agregar al carrito Sin log</div>
                    <% } %>
                  </div>
               
            </div>
        </section>
        <section class="productDescription">
            <div class="description">
                <h5>Descripción del producto</h5>
                <p><%= product.description %> </p>
            </div>
            
        </section>
        <section>
          <!-- /* Slider */ -->  
          <%- include('partials/slider.ejs') %>
        </section>
    </main>
    <!-- /* FOOTER */ -->
    <%- include('partials/footer.ejs')%>
  
      
    <!-- JavaScript -->
    <script src="/js/productDetail.js"></script>
    <script src="/js/carrito.js"></script>
    <script src="https://unpkg.com/flickity@2/dist/flickity.pkgd.min.js"></script>
</body>
</html>